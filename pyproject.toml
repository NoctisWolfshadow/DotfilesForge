[project]
name = "dotfilesforge"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "NoctisWolfshadow", email = "basilgarrad@yahoo.de" }
]
requires-python = ">=3.11"
dependencies = [
    "click>=8.3.3",
    "gitpython>=3.1.46",
    "packaging>=26.0",
    "rich>=14.3.3",
    "typing-extensions>=4.15.0 ; python_full_version < '3.12'",
]

[project.scripts]
dotfilesforge = "dotfilesforge.cli:cli"

[build-system]
requires = ["uv_build>=0.10.7,<0.12.0"]
build-backend = "uv_build"
